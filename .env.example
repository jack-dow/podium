# Supabase settings
# Can be found in Settings > API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Prisma DB URLs - with supabase can be found in Settings > Database
# Ensure you set the port to 6543 on this page too!
# DATABASE_URL=

# This is the direct connection URL that does not go through PgBouncer. Use this when trying to npx prisma db pull
# As prisma cannot do that through PgBouncer
DATABASE_URL=
